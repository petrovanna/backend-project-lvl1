name: Linter

on: [push]

jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest

    steps:
      - run: make install
      
      - name: Check out repository code
        uses: actions/checkout@v3

      - name: Linter
        run: make lint
